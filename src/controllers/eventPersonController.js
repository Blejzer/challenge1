'use strict';

const Controller = require('./mainController')

class EventPersonController extends Controller {
    constructor(model) {
        super(model);
    }
}

module.exports = EventPersonController